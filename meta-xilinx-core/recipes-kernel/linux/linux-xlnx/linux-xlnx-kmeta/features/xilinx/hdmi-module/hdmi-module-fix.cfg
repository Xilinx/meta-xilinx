# CONFIG_SND_HDA_PREALLOC_SIZE depends on CONFIG_SND_HDA which is not
# enabled on aarch64 platforms. The upstream hdmi-module.cfg sets it
# without enabling the parent dependency.
# CONFIG_SND_HDA_PREALLOC_SIZE is not set
